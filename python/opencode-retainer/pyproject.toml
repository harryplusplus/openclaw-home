[project]
name = "opencode-retainer"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "platformdirs>=4.5.0",
    "pydantic>=2.12.5",
    "rich>=14.3.4",
]

[project.scripts]
opencode-retainer = "opencode_retainer.cli:main"

[tool.uv]
package = true
